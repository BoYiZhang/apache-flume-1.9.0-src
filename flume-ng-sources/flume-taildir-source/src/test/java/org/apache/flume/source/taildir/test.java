package org.apache.flume.source.taildir;

import java.util.concurrent.TimeUnit;

public class test {
    public static void main(String[] args) {
//        long now = TimeUnit.SECONDS.toMillis(
//                TimeUnit.MILLISECONDS.toSeconds(System.currentTimeMillis()));
//
//        System.out.println(System.currentTimeMillis());
//        System.out.println(TimeUnit.MILLISECONDS.toSeconds(System.currentTimeMillis()));
//        System.out.println(TimeUnit.SECONDS.toMillis(TimeUnit.MILLISECONDS.toSeconds(System.currentTimeMillis())));




//        byte[]  data = "\n".getBytes();
//
//        for (byte b : data) {
//            System.out.println(b);
//        }



        for(int i= 0 ; i < 1000 ; i++){
            System.out.println(i+"@正正正正正正正正正正");
        }
    }
}
